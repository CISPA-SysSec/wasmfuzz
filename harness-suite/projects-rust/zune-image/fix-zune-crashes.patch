diff --git a/crates/zune-core/src/bytestream/reader/no_std_readers.rs b/crates/zune-core/src/bytestream/reader/no_std_readers.rs
index dd1c2b9..2a57865 100644
--- a/crates/zune-core/src/bytestream/reader/no_std_readers.rs
+++ b/crates/zune-core/src/bytestream/reader/no_std_readers.rs
@@ -69,12 +69,16 @@ impl<T: AsRef<[u8]>>  ZCursor<T> {
     }
 
     pub (crate) fn read_const_bytes_impl<const N: usize>(&mut self, buf: &mut [u8; N]) -> Result<(), ZByteIoError> {
-        if self.position + N <= self.stream.as_ref().len() {
+        let end = self
+            .position
+            .checked_add(N)
+            .ok_or(ZByteIoError::Generic("Read position overflow"))?;
+        if end <= self.stream.as_ref().len() {
             // we are in bounds
             let reference = self.stream.as_ref();
             let position = self.position;
-            if let Some(buf_ref) = reference.get(position..position + N) {
-                self.position += N;
+            if let Some(buf_ref) = reference.get(position..end) {
+                self.position = end;
                 buf.copy_from_slice(buf_ref);
                 return Ok(());
             }
@@ -83,13 +87,14 @@ impl<T: AsRef<[u8]>>  ZCursor<T> {
     }
 
     pub (crate) fn read_const_bytes_no_error_impl<const N: usize>(&mut self, buf: &mut [u8; N]) {
-        if self.position + N <= self.stream.as_ref().len() {
-            // we are in bounds
-            let reference = self.stream.as_ref();
-            let position = self.position;
-            if let Some(buf_ref) = reference.get(position..position + N) {
-                self.position += N;
-                buf.copy_from_slice(buf_ref);
+        if let Some(end) = self.position.checked_add(N) {
+            if end <= self.stream.as_ref().len() {
+                let reference = self.stream.as_ref();
+                let position = self.position;
+                if let Some(buf_ref) = reference.get(position..end) {
+                    self.position = end;
+                    buf.copy_from_slice(buf_ref);
+                }
             }
         }
     }
@@ -106,13 +111,23 @@ impl<T: AsRef<[u8]>>  ZCursor<T> {
         let stream_end = self.stream.as_ref().len();
 
         let start = core::cmp::min(self.position, stream_end);
-        let end = core::cmp::min(self.position + buf.len(), stream_end);
-
-        let slice = self.stream.as_ref().get(start..end).unwrap();
+        let end_exclusive = self
+            .position
+            .checked_add(buf.len())
+            .ok_or(ZByteIoError::Generic("Read position overflow"))?;
+        let end = core::cmp::min(end_exclusive, stream_end);
+
+        if start >= end {
+            return Ok(0);
+        }
+        let slice = self
+            .stream
+            .as_ref()
+            .get(start..end)
+            .ok_or(ZByteIoError::Generic("Invalid peek range"))?;
         buf[..slice.len()].copy_from_slice(slice);
-        let len = slice.len();
 
-        Ok(len)
+        Ok(slice.len())
     }
 
     #[inline(always)]
diff --git a/crates/zune-hdr/src/decoder.rs b/crates/zune-hdr/src/decoder.rs
index 6c07f99..b9055ce 100644
--- a/crates/zune-hdr/src/decoder.rs
+++ b/crates/zune-hdr/src/decoder.rs
@@ -14,7 +14,7 @@ use core::iter::Iterator;
 use core::option::Option::{self, *};
 use core::result::Result::{self, *};
 
-use zune_core::bytestream::{ZByteReaderTrait, ZReader};
+use zune_core::bytestream::{ZByteIoError, ZByteReaderTrait, ZReader};
 use zune_core::colorspace::ColorSpace;
 use zune_core::log::trace;
 use zune_core::options::DecoderOptions;
@@ -247,7 +247,20 @@ where
     ///   error occurred during decoding.
     pub fn decode(&mut self) -> Result<Vec<f32>, HdrDecodeErrors> {
         self.decode_headers()?;
-        let mut buffer = vec![0.0f32; self.width * self.height * 3];
+        let Some(elem_count) = self.output_buffer_size() else {
+            return Err(HdrDecodeErrors::Generic(
+                "Could not compute HDR output buffer size",
+            ));
+        };
+        let byte_size = elem_count
+            .checked_mul(core::mem::size_of::<f32>())
+            .ok_or(HdrDecodeErrors::Generic("HDR output size overflow"))?;
+        if byte_size > self.options.inflate_get_limit() {
+            return Err(HdrDecodeErrors::IoErrors(ZByteIoError::from(
+                "Decoded HDR output exceeds configured decoder limit",
+            )));
+        }
+        let mut buffer = vec![0.0f32; elem_count];
 
         self.decode_into(&mut buffer)?;
 
diff --git a/crates/zune-png/src/headers/readers.rs b/crates/zune-png/src/headers/readers.rs
index 7e3652f..d9a8c46 100644
--- a/crates/zune-png/src/headers/readers.rs
+++ b/crates/zune-png/src/headers/readers.rs
@@ -293,7 +293,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
     }
 
     pub(crate) fn parse_exif(&mut self, chunk: PngChunk) -> Result<(), PngDecodeErrors> {
-        let data = self.stream.peek_at(0, chunk.length).unwrap();
+        let data = self.stream.peek_at(0, chunk.length)?;
 
         // recommended that we check for first four bytes compatibility
         // so do it here
@@ -321,7 +321,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
     /// Parse the iCCP chunk
     pub(crate) fn parse_iccp(&mut self, chunk: PngChunk) -> Result<(), PngDecodeErrors> {
         let length = core::cmp::min(chunk.length, 79);
-        let keyword_bytes = self.stream.peek_at(0, length).unwrap();
+        let keyword_bytes = self.stream.peek_at(0, length)?;
         let keyword_position = keyword_bytes.iter().position(|x| *x == 0);
 
         if let Some(pos) = keyword_position {
@@ -338,7 +338,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
             let _ = self.stream.read_u8();
 
             // read remaining chunk
-            let data = self.stream.peek_at(0, remainder).unwrap();
+            let data = self.stream.peek_at(0, remainder)?;
 
             // decode to vec
             if let Ok(icc_uncompressed) = DeflateDecoder::new(data).decode_zlib() {
@@ -360,7 +360,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
     /// Parse the text chunk
     pub(crate) fn parse_text(&mut self, chunk: PngChunk) -> Result<(), PngDecodeErrors> {
         let length = core::cmp::min(chunk.length, 79);
-        let keyword_bytes = self.stream.peek_at(0, length).unwrap();
+        let keyword_bytes = self.stream.peek_at(0, length)?;
         let keyword_position = keyword_bytes.iter().position(|x| *x == 0);
 
         if let Some(pos) = keyword_position {
@@ -372,7 +372,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
 
             // read remaining chunk
 
-            let text = self.stream.peek_at(0, remainder).unwrap().to_vec();
+            let text = self.stream.peek_at(0, remainder)?.to_vec();
 
             let text_chunk = TextChunk { keyword, text };
             self.png_info.text_chunk.push(text_chunk);
@@ -390,7 +390,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
     /// Parse the itXT chunk
     pub(crate) fn parse_itxt(&mut self, chunk: PngChunk) -> Result<(), PngDecodeErrors> {
         let length = core::cmp::min(chunk.length, 79);
-        let keyword_bytes = self.stream.peek_at(0, length).unwrap();
+        let keyword_bytes = self.stream.peek_at(0, length)?;
         let keyword_position = keyword_bytes.iter().position(|x| *x == 0);
 
         if let Some(pos) = keyword_position {
@@ -404,7 +404,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
 
             self.stream.skip(bytes_to_skip)?;
             let remainder = chunk.length.saturating_sub(bytes_to_skip);
-            let raw_data = self.stream.peek_at(0, remainder).unwrap().to_vec();
+            let raw_data = self.stream.peek_at(0, remainder)?.to_vec();
 
             let itxt_chunk = ItxtChunk {
                 keyword,
@@ -425,7 +425,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
     /// Parse zTxt chunk
     pub(crate) fn parse_ztxt(&mut self, chunk: PngChunk) -> Result<(), PngDecodeErrors> {
         let length = core::cmp::min(chunk.length, 79);
-        let keyword_bytes = self.stream.peek_at(0, length).unwrap();
+        let keyword_bytes = self.stream.peek_at(0, length)?;
         let keyword_position = keyword_bytes.iter().position(|x| *x == 0);
 
         if let Some(pos) = keyword_position {
@@ -444,7 +444,7 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
             let _ = self.stream.read_u8();
 
             // read remaining chunk
-            let data = self.stream.peek_at(0, remainder).unwrap();
+            let data = self.stream.peek_at(0, remainder)?;
 
             // decode to vec
             if let Ok(ztxt) = DeflateDecoder::new(data).decode_zlib() {
@@ -509,12 +509,24 @@ impl<T: ZByteReaderTrait> PngDecoder<T> {
                 }
                 // get frame data
                 // skip four  bytes since it's usually sequence number
-                let chunk = self.frames.last_mut().unwrap().chunk_mut();
+                let Some(frame) = self.frames.last_mut() else {
+                    return Err(PngDecodeErrors::GenericStatic(
+                        "APNG fdAT chunk without a frame buffer",
+                    ));
+                };
+                let chunk = frame.chunk_mut();
                 let prev_len = chunk.len();
                 // skip the header
                 self.stream.skip(4)?;
+                let add_len = next_header.length.saturating_sub(4);
+                let new_len = chunk
+                    .len()
+                    .checked_add(add_len)
+                    .ok_or(PngDecodeErrors::GenericStatic(
+                        "APNG frame chunk size overflow",
+                    ))?;
                 // allocate space for header ignoring content
-                chunk.resize(chunk.len() + next_header.length.saturating_sub(4), 0);
+                chunk.resize(new_len, 0);
 
                 self.stream.read_exact_bytes(&mut chunk[prev_len..])?;
                 // skip crc
diff --git a/crates/zune-ppm/src/decoder.rs b/crates/zune-ppm/src/decoder.rs
index 55ac7da..9775a19 100644
--- a/crates/zune-ppm/src/decoder.rs
+++ b/crates/zune-ppm/src/decoder.rs
@@ -12,7 +12,7 @@ use alloc::{format, vec};
 use core::fmt::{Debug, Formatter};
 
 use zune_core::bit_depth::{BitDepth, BitType, ByteEndian};
-use zune_core::bytestream::{ZByteIoError, ZByteReaderTrait, ZReader};
+use zune_core::bytestream::{ZByteIoError, ZByteReaderTrait, ZReader, ZSeekFrom};
 use zune_core::colorspace::ColorSpace;
 use zune_core::log::trace;
 use zune_core::options::DecoderOptions;
@@ -427,10 +427,15 @@ where
             let byte = self.reader.read_u8();
 
             if byte.is_ascii_digit() {
-                // if it overflows, we have bigger problems.
+                let digit = usize::from(byte - b'0');
                 value = value
-                    .wrapping_mul(10_usize)
-                    .wrapping_add(usize::from(byte - b'0'))
+                    .checked_mul(10)
+                    .and_then(|v| v.checked_add(digit))
+                    .ok_or_else(|| {
+                        PPMDecodeErrors::InvalidHeader(
+                            "Integer overflow while parsing PPM header field".to_string()
+                        )
+                    })?;
             } else {
                 // rewind to the previous byte
                 self.reader.rewind(1)?;
@@ -440,6 +445,17 @@ where
         Ok(value)
     }
 
+    /// Bytes left in the input from the current read position to end-of-stream.
+    fn input_raster_bytes_remaining(&mut self) -> Result<usize, PPMDecodeErrors> {
+        let cur = self.reader.position()?;
+        let stream_end = self.reader.seek(ZSeekFrom::End(0))?;
+        self.reader.seek(ZSeekFrom::Start(cur))?;
+        let remaining = stream_end.saturating_sub(cur);
+        usize::try_from(remaining).map_err(|_| {
+            PPMDecodeErrors::GenericStatic("Stream length does not fit in usize")
+        })
+    }
+
     /// Return the image bit depth or none if headers
     /// are not decoded.
     ///
@@ -537,9 +553,23 @@ where
                 "Zero dimensions not allowed"
             ));
         }
-        // okay check if the stream is large enough for the bit depth
-        let size =
-            self.width * self.height * self.colorspace.num_components() * self.bit_depth.size_of();
+        let components = self.colorspace.num_components();
+        let bytes_per_component = self.bit_depth.size_of();
+        let size = self
+            .width
+            .checked_mul(self.height)
+            .and_then(|n| n.checked_mul(components))
+            .and_then(|n| n.checked_mul(bytes_per_component))
+            .ok_or(PPMDecodeErrors::GenericStatic(
+                "Decoded image dimensions overflow buffer size computation"
+            ))?;
+
+        let available = self.input_raster_bytes_remaining()?;
+        if size > available {
+            return Err(PPMDecodeErrors::IoErrors(ZByteIoError::NotEnoughBytes(
+                available, size
+            )));
+        }
 
         match self.bit_depth.bit_type() {
             BitType::U8 => {
diff --git a/crates/zune-psd/src/decoder.rs b/crates/zune-psd/src/decoder.rs
index c41f782..4a0a14e 100644
--- a/crates/zune-psd/src/decoder.rs
+++ b/crates/zune-psd/src/decoder.rs
@@ -261,6 +261,11 @@ where
         }
 
         let required_length = self.required_len()?;
+        if required_length > self.options.inflate_get_limit() {
+            return Err(PSDDecodeErrors::Generic(
+                "Decoded PSD raster exceeds configured decoder size limit",
+            ));
+        }
         let mut out = vec![0; required_length];
         let new_length = self.decode_into(&mut out)?;
         debug_assert!(new_length == required_length);
@@ -389,14 +394,17 @@ where
             self.decode_headers()?;
         }
 
-        let pixel_count = self.width * self.height;
-        let bytes_per_sample = match self.depth {
-            BitDepth::Eight => 1,
-            BitDepth::Sixteen => 2,
-            _ => return Err(PSDDecodeErrors::Generic("Unsupported bit depth")),
-        };
+        let pixel_count = self
+            .width
+            .checked_mul(self.height)
+            .ok_or(PSDDecodeErrors::Generic("Dimension overflow"))?;
 
-        let required_len = pixel_count * self.channel_count * bytes_per_sample;
+        let required_len = self.required_len()?;
+        if required_len > self.options.inflate_get_limit() {
+            return Err(PSDDecodeErrors::Generic(
+                "Decoded PSD raster exceeds configured decoder size limit",
+            ));
+        }
 
         if output.len() < required_len {
             return Err(PSDDecodeErrors::Generic("Output buffer too small"));
@@ -406,7 +414,11 @@ where
 
         match (self.compression, self.depth) {
             (CompressionMethod::RLE, BitDepth::Eight) => {
-                let skipped = self.height * self.channel_count * 2;
+                let skipped = self
+                    .height
+                    .checked_mul(self.channel_count)
+                    .and_then(|v| v.checked_mul(2))
+                    .ok_or(PSDDecodeErrors::Generic("RLE header size overflow"))?;
                 self.stream.skip(skipped)?;
 
                 for channel in 0..self.channel_count {
diff --git a/crates/zune-qoi/src/decoder.rs b/crates/zune-qoi/src/decoder.rs
index 04e2902..cb9c96e 100644
--- a/crates/zune-qoi/src/decoder.rs
+++ b/crates/zune-qoi/src/decoder.rs
@@ -10,7 +10,7 @@ use alloc::vec::Vec;
 use alloc::{format, vec};
 
 use zune_core::bit_depth::BitDepth;
-use zune_core::bytestream::{ZByteReaderTrait, ZReader};
+use zune_core::bytestream::{ZByteReaderTrait, ZReader, ZSeekFrom};
 use zune_core::colorspace::ColorSpace;
 use zune_core::log::{error, trace};
 use zune_core::options::DecoderOptions;
@@ -194,14 +194,19 @@ where
     /// # Panics
     /// In case `width*height*colorspace` calculation may overflow a usize
     pub fn output_buffer_size(&self) -> Option<usize> {
-        if self.decoded_headers {
-            self.width
-                .checked_mul(self.height)
-                .unwrap()
-                .checked_mul(self.colorspace.num_components())
-        } else {
-            None
+        if !self.decoded_headers {
+            return None;
         }
+        let wh = self.width.checked_mul(self.height)?;
+        wh.checked_mul(self.colorspace.num_components())
+    }
+
+    fn qoi_payload_bytes_remaining(&mut self) -> Result<usize, QoiErrors> {
+        let cur = self.stream.position()?;
+        let stream_end = self.stream.seek(ZSeekFrom::End(0))?;
+        self.stream.seek(ZSeekFrom::Start(cur))?;
+        let remaining = stream_end.saturating_sub(cur);
+        usize::try_from(remaining).map_err(|_| QoiErrors::GenericStatic("stream length overflow"))
     }
 
     /// Decode the bytes of a QOI image data, returning the
@@ -223,7 +228,28 @@ where
         if !self.decoded_headers {
             self.decode_headers()?;
         }
-        let mut output = vec![0; self.output_buffer_size().unwrap()];
+        let Some(output_size) = self.output_buffer_size() else {
+            return Err(QoiErrors::GenericStatic(
+                "Could not compute QOI output buffer size"
+            ));
+        };
+        if output_size > self.options.inflate_get_limit() {
+            return Err(QoiErrors::Generic(format!(
+                "Decoded QOI output size {output_size} exceeds configured limit {}",
+                self.options.inflate_get_limit()
+            )));
+        }
+        let pixels = self
+            .width
+            .checked_mul(self.height)
+            .ok_or(QoiErrors::GenericStatic("QOI dimensions overflow"))?;
+        let min_payload = pixels.div_ceil(64).saturating_add(8);
+        let avail = self.qoi_payload_bytes_remaining()?;
+        if avail < min_payload {
+            return Err(QoiErrors::InsufficientData(min_payload, avail));
+        }
+
+        let mut output = vec![0; output_size];
 
         self.decode_into(&mut output)?;
 
@@ -248,11 +274,13 @@ where
             self.decode_headers()?;
         }
 
-        if pixels.len() < self.output_buffer_size().unwrap() {
-            return Err(QoiErrors::InsufficientData(
-                self.output_buffer_size().unwrap(),
-                pixels.len()
+        let Some(expected) = self.output_buffer_size() else {
+            return Err(QoiErrors::GenericStatic(
+                "Could not compute QOI output buffer size"
             ));
+        };
+        if pixels.len() < expected {
+            return Err(QoiErrors::InsufficientData(expected, pixels.len()));
         }
 
         match self.colorspace.num_components() {
