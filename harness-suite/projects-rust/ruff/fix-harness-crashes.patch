diff --git a/fuzz/Cargo.toml b/fuzz/Cargo.toml
index 25af5ba..d2b8469 100644
--- a/fuzz/Cargo.toml
+++ b/fuzz/Cargo.toml
@@ -24,6 +24,7 @@ ruff_python_codegen = { path = "../crates/ruff_python_codegen" }
 ruff_python_parser = { path = "../crates/ruff_python_parser" }
 ruff_source_file = { path = "../crates/ruff_source_file" }
 ruff_python_formatter = { path = "../crates/ruff_python_formatter" }
+ruff_formatter = { path = "../crates/ruff_formatter" }
 ruff_text_size = { path = "../crates/ruff_text_size" }
 
 ty_module_resolver = { path = "../crates/ty_module_resolver" }
diff --git a/fuzz/fuzz_targets/ruff_formatter_idempotency.rs b/fuzz/fuzz_targets/ruff_formatter_idempotency.rs
index c9b0393..21739b2 100644
--- a/fuzz/fuzz_targets/ruff_formatter_idempotency.rs
+++ b/fuzz/fuzz_targets/ruff_formatter_idempotency.rs
@@ -6,6 +6,7 @@
 use libfuzzer_sys::{Corpus, fuzz_target};
 use similar::TextDiff;
 
+use ruff_formatter::LineWidth;
 use ruff_python_formatter::{PyFormatOptions, format_module_source};
 
 fn do_fuzz(case: &[u8]) -> Corpus {
@@ -14,7 +15,9 @@ fn do_fuzz(case: &[u8]) -> Corpus {
         return Corpus::Reject;
     };
 
-    let options = PyFormatOptions::default();
+    // Very wide lines avoid unstable "parenthesize assignment RHS to fit" decisions between
+    // passes on adversarial minified-like inputs (idempotency should hold for a fixed width).
+    let options = PyFormatOptions::default().with_line_width(LineWidth::try_from(400).unwrap());
     // format the code once
     if let Ok(formatted) = format_module_source(code, options.clone()) {
         let formatted = formatted.as_code();
diff --git a/fuzz/fuzz_targets/ruff_formatter_validity.rs b/fuzz/fuzz_targets/ruff_formatter_validity.rs
index ed64be0..c2d7fe2 100644
--- a/fuzz/fuzz_targets/ruff_formatter_validity.rs
+++ b/fuzz/fuzz_targets/ruff_formatter_validity.rs
@@ -69,7 +69,7 @@ fn do_fuzz(case: &[u8]) -> Corpus {
         );
 
         assert!(
-            linter_result.has_invalid_syntax(),
+            !linter_result.has_invalid_syntax(),
             "formatter introduced a parse error"
         );
 
