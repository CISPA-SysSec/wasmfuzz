diff --git a/crates/jxl-frame/src/data/pass_group.rs b/crates/jxl-frame/src/data/pass_group.rs
index c518253..228e067 100644
--- a/crates/jxl-frame/src/data/pass_group.rs
+++ b/crates/jxl-frame/src/data/pass_group.rs
@@ -72,13 +72,21 @@ pub fn decode_pass_group<S: Sample>(
 
         let block_left = lf_col * group_dim_blocks;
         let block_top = lf_row * group_dim_blocks;
-        let block_width = (block_info.width() - block_left).min(group_dim_blocks);
-        let block_height = (block_info.height() - block_top).min(group_dim_blocks);
+        let block_width = block_info
+            .width()
+            .saturating_sub(block_left)
+            .min(group_dim_blocks);
+        let block_height = block_info
+            .height()
+            .saturating_sub(block_top)
+            .min(group_dim_blocks);
 
         let jpeg_upsampling = frame_header.jpeg_upsampling;
+        let block_right = block_left.saturating_add(block_width).min(block_info.width());
+        let block_bottom = block_top.saturating_add(block_height).min(block_info.height());
         let block_info = block_info.as_subgrid().subgrid(
-            block_left..(block_left + block_width),
-            block_top..(block_top + block_height),
+            block_left..block_right,
+            block_top..block_bottom,
         );
         let lf_quant: Option<[_; 3]> = lf_group.lf_coeff.as_ref().map(|lf_coeff| {
             let lf_quant_channels = lf_coeff.lf_quant.image().unwrap().image_channels();
@@ -90,10 +98,14 @@ pub fn decode_pass_group<S: Sample>(
                 let block_top = block_top >> shift.vshift();
                 let (block_width, block_height) =
                     shift.shift_size((block_width as u32, block_height as u32));
-                lf_quant.as_subgrid().subgrid(
-                    block_left..(block_left + block_width as usize),
-                    block_top..(block_top + block_height as usize),
-                )
+                let lf_quant_grid = lf_quant.as_subgrid();
+                let quant_right = block_left
+                    .saturating_add(block_width as usize)
+                    .min(lf_quant_grid.width());
+                let quant_bottom = block_top
+                    .saturating_add(block_height as usize)
+                    .min(lf_quant_grid.height());
+                lf_quant_grid.subgrid(block_left..quant_right, block_top..quant_bottom)
             })
         });
 
diff --git a/crates/jxl-render/src/filter/ycbcr.rs b/crates/jxl-render/src/filter/ycbcr.rs
index 027fa68..c10480a 100644
--- a/crates/jxl-render/src/filter/ycbcr.rs
+++ b/crates/jxl-render/src/filter/ycbcr.rs
@@ -14,14 +14,39 @@ pub fn apply_jpeg_upsampling_single(
     }
 
     let height = grid.height();
+    let width = grid.width();
     let target_width = target_region.width as usize;
     let target_height = target_region.height as usize;
 
-    let mut out = AlignedGrid::with_alloc_tracker(target_width, target_height, tracker)?;
-    let buf = out.buf_mut();
-
     let h_upsampled = shift.hshift() == 0;
     let v_upsampled = shift.vshift() == 0;
+    if h_upsampled && target_width > width {
+        return Err(jxl_bitstream::Error::ValidationFailed(
+            "jpeg upsample target wider than source grid",
+        )
+        .into());
+    }
+    if !h_upsampled && target_width > width.saturating_mul(2) {
+        return Err(jxl_bitstream::Error::ValidationFailed(
+            "jpeg upsample target wider than 2x source grid",
+        )
+        .into());
+    }
+    if v_upsampled && target_height > height {
+        return Err(jxl_bitstream::Error::ValidationFailed(
+            "jpeg upsample target taller than source grid",
+        )
+        .into());
+    }
+    if !v_upsampled && target_height > height.saturating_mul(2) {
+        return Err(jxl_bitstream::Error::ValidationFailed(
+            "jpeg upsample target taller than 2x source grid",
+        )
+        .into());
+    }
+
+    let mut out = AlignedGrid::with_alloc_tracker(target_width, target_height, tracker)?;
+    let buf = out.buf_mut();
 
     for (y, out_row) in buf.chunks_exact_mut(target_width).take(height).enumerate() {
         let row = grid.get_row(y);
diff --git a/crates/jxl-render/src/image.rs b/crates/jxl-render/src/image.rs
index ecd4327..8ff6e6b 100644
--- a/crates/jxl-render/src/image.rs
+++ b/crates/jxl-render/src/image.rs
@@ -465,10 +465,18 @@ impl ImageWithRegion {
             let width = downsampled_valid_region.width;
             let height = downsampled_valid_region.height;
 
-            let subgrid = g.as_float().unwrap().as_subgrid().subgrid(
-                left as usize..(left + width) as usize,
-                top as usize..(top + height) as usize,
-            );
+            let grid = g.as_float().unwrap();
+            let l = left as usize;
+            let t = top as usize;
+            let r = l.saturating_add(width as usize).min(grid.width());
+            let b = t.saturating_add(height as usize).min(grid.height());
+            if l >= r || t >= b {
+                return Err(jxl_bitstream::Error::ValidationFailed(
+                    "jpeg upsample subgrid out of bounds",
+                )
+                .into());
+            }
+            let subgrid = grid.as_subgrid().subgrid(l..r, t..b);
             let out = crate::filter::apply_jpeg_upsampling_single(
                 subgrid,
                 *shift,
@@ -527,10 +535,17 @@ impl ImageWithRegion {
             let width = downsampled_valid_region.width;
             let height = downsampled_valid_region.height;
 
-            let subgrid = grid.as_subgrid().subgrid(
-                left as usize..(left + width) as usize,
-                top as usize..(top + height) as usize,
-            );
+            let l = left as usize;
+            let t = top as usize;
+            let r = l.saturating_add(width as usize).min(grid.width());
+            let b = t.saturating_add(height as usize).min(grid.height());
+            if l >= r || t >= b {
+                return Err(jxl_bitstream::Error::ValidationFailed(
+                    "non-separable upsample subgrid out of bounds",
+                )
+                .into());
+            }
+            let subgrid = grid.as_subgrid().subgrid(l..r, t..b);
             *region = downsampled_valid_region;
             let out = tracing::trace_span!(
                 "Non-separable upsampling",
diff --git a/crates/jxl-render/src/vardct/mod.rs b/crates/jxl-render/src/vardct/mod.rs
index 61c1596..9b6389d 100644
--- a/crates/jxl-render/src/vardct/mod.rs
+++ b/crates/jxl-render/src/vardct/mod.rs
@@ -509,11 +509,19 @@ pub fn dequant_hf_varblock_grouped<S: Sample>(
     };
 
     let block_info = &hf_meta.block_info;
-    let lf_width = (block_info.width() - left_in_lf).min(group_dim as usize / 8);
-    let lf_height = (block_info.height() - top_in_lf).min(group_dim as usize / 8);
+    let lf_width = block_info
+        .width()
+        .saturating_sub(left_in_lf)
+        .min(group_dim as usize / 8);
+    let lf_height = block_info
+        .height()
+        .saturating_sub(top_in_lf)
+        .min(group_dim as usize / 8);
+    let block_right = left_in_lf.saturating_add(lf_width).min(block_info.width());
+    let block_bottom = top_in_lf.saturating_add(lf_height).min(block_info.height());
     let block_info = hf_meta.block_info.as_subgrid().subgrid(
-        left_in_lf..(left_in_lf + lf_width),
-        top_in_lf..(top_in_lf + lf_height),
+        left_in_lf..block_right,
+        top_in_lf..block_bottom,
     );
 
     for (channel, coeff) in out.iter_mut().enumerate() {
@@ -547,9 +555,13 @@ pub fn dequant_hf_varblock_grouped<S: Sample>(
                     dequant_matrices.get(channel, dct_select)
                 };
 
-                let mut coeff = coeff
-                    .borrow_mut()
-                    .subgrid(left..(left + width), top..(top + height));
+                let coeff_grid = coeff.borrow_mut();
+                let coeff_right = left.saturating_add(width).min(coeff_grid.width());
+                let coeff_bottom = top.saturating_add(height).min(coeff_grid.height());
+                if left >= coeff_right || top >= coeff_bottom {
+                    return;
+                }
+                let mut coeff = coeff_grid.subgrid(left..coeff_right, top..coeff_bottom);
                 for (y, matrix_row) in matrix.chunks_exact(width).enumerate() {
                     let row = coeff.get_row_mut(y);
                     for (q, &m) in row.iter_mut().zip(matrix_row) {
@@ -666,18 +678,28 @@ pub fn transform_with_lf_grouped<S: Sample>(
     .map(|((lf_region, shift), lf)| {
         let lf_base_left = lf_base_left.checked_add_signed(-lf_region.left).unwrap();
         let lf_base_top = lf_base_top.checked_add_signed(-lf_region.top).unwrap();
-        let lf_width = (lf_region.width - lf_base_left).min(group_width.div_ceil(8));
-        let lf_height = (lf_region.height - lf_base_top).min(group_height.div_ceil(8));
+        let lf_width = lf_region
+            .width
+            .saturating_sub(lf_base_left)
+            .min(group_width.div_ceil(8));
+        let lf_height = lf_region
+            .height
+            .saturating_sub(lf_base_top)
+            .min(group_height.div_ceil(8));
         let lf_base_left = lf_base_left as usize;
         let lf_base_top = lf_base_top as usize;
 
         let lf_base_left = lf_base_left >> shift.hshift();
         let lf_base_top = lf_base_top >> shift.vshift();
         let (lf_width, lf_height) = shift.shift_size((lf_width, lf_height));
-        lf.as_subgrid().subgrid(
-            lf_base_left..(lf_base_left + lf_width as usize),
-            lf_base_top..(lf_base_top + lf_height as usize),
-        )
+        let lf_subgrid = lf.as_subgrid();
+        let lf_right = lf_base_left
+            .saturating_add(lf_width as usize)
+            .min(lf_subgrid.width());
+        let lf_bottom = lf_base_top
+            .saturating_add(lf_height as usize)
+            .min(lf_subgrid.height());
+        lf_subgrid.subgrid(lf_base_left..lf_right, lf_base_top..lf_bottom)
     });
 
     let lf_group_idx = frame_header.lf_group_idx_from_group_idx(group_idx);
@@ -716,13 +738,23 @@ pub fn transform_with_lf_grouped<S: Sample>(
         let lf_region = lf_regions[0].0;
         let lf_base_left = lf_base_left.checked_add_signed(-lf_region.left).unwrap();
         let lf_base_top = lf_base_top.checked_add_signed(-lf_region.top).unwrap();
-        let lf_width = (lf_region.width - lf_base_left).min(group_width.div_ceil(8));
-        let lf_height = (lf_region.height - lf_base_top).min(group_height.div_ceil(8));
-
-        hf_meta.block_info.as_subgrid().subgrid(
-            left_in_lf..(left_in_lf + lf_width as usize),
-            top_in_lf..(top_in_lf + lf_height as usize),
-        )
+        let lf_width = lf_region
+            .width
+            .saturating_sub(lf_base_left)
+            .min(group_width.div_ceil(8));
+        let lf_height = lf_region
+            .height
+            .saturating_sub(lf_base_top)
+            .min(group_height.div_ceil(8));
+
+        let block_info_grid = hf_meta.block_info.as_subgrid();
+        let block_right = left_in_lf
+            .saturating_add(lf_width as usize)
+            .min(block_info_grid.width());
+        let block_bottom = top_in_lf
+            .saturating_add(lf_height as usize)
+            .min(block_info_grid.height());
+        block_info_grid.subgrid(left_in_lf..block_right, top_in_lf..block_bottom)
     };
 
     impls::transform_varblocks(&lf, coeff_out, shifts_cbycr, &block_info);
