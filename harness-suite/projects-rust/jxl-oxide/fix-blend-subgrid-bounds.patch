diff --git a/crates/jxl-render/src/blend.rs b/crates/jxl-render/src/blend.rs
--- a/crates/jxl-render/src/blend.rs
+++ b/crates/jxl-render/src/blend.rs
@@ -295,6 +295,13 @@ pub(crate) fn blend<S: Sample>(
                     } = region;
                     let right = left.wrapping_add_unsigned(width);
                     let bottom = top.wrapping_add_unsigned(height);
+                    if left < 0 || top < 0
+                        || right < 0 || bottom < 0
+                        || right as usize > target_grid.width()
+                        || bottom as usize > target_grid.height()
+                    {
+                        return Err(jxl_bitstream::Error::ValidationFailed("blend target subgrid out of bounds").into());
+                    }
                     target_grid
                         .as_subgrid_mut()
                         .subgrid(left as usize..right as usize, top as usize..bottom as usize)
@@ -320,6 +327,13 @@ pub(crate) fn blend<S: Sample>(
                         } = region;
                         let right = left.wrapping_add_unsigned(width);
                         let bottom = top.wrapping_add_unsigned(height);
+                        if left < 0 || top < 0
+                            || right < 0 || bottom < 0
+                            || right as usize > base_alpha_grid.width()
+                            || bottom as usize > base_alpha_grid.height()
+                        {
+                            return Err(jxl_bitstream::Error::ValidationFailed("blend base alpha subgrid out of bounds").into());
+                        }
                         base_alpha_grid
                             .as_subgrid()
                             .subgrid(left as usize..right as usize, top as usize..bottom as usize)
