diff --git a/jxl/src/frame/modular/transforms/squeeze.rs b/jxl/src/frame/modular/transforms/squeeze.rs
index d85ebb0..28cb4a5 100644
--- a/jxl/src/frame/modular/transforms/squeeze.rs
+++ b/jxl/src/frame/modular/transforms/squeeze.rs
@@ -801,7 +801,9 @@ fn do_hsqueeze_step_i16(
     scratch_i16: &mut [i16; 2048],
 ) {
     trace!("hsqueeze step in_avg: {in_avg:?} in_res: {in_res:?} in_next_avg: {in_next_avg:?}");
-    let out = buffers.first_mut().unwrap();
+    let Some(out) = buffers.first_mut() else {
+        return;
+    };
     let mut out_rect = ImageRectMut::<i16>::from_raw(out.data.as_rect_mut());
     // Shortcut: guarantees that row is at least 1px in the main loop
     if out_rect.size().0 == 0 || out_rect.size().1 == 0 {
@@ -811,8 +813,11 @@ fn do_hsqueeze_step_i16(
     let w = in_res.size().0;
     // Another shortcut: when output row has just 1px
     if w == 0 {
+        if in_avg.size().0 == 0 || in_avg.size().1 == 0 {
+            return;
+        }
         let out_h = out_rect.size().1;
-        for y in 0..out_h {
+        for y in 0..out_h.min(in_avg.size().1) {
             out_rect.row(y)[0] = in_avg.row(y)[0];
         }
         return;
@@ -837,7 +842,9 @@ fn do_hsqueeze_step_i32(
     buffers: &mut [&mut ModularChannel],
 ) {
     trace!("hsqueeze step in_avg: {in_avg:?} in_res: {in_res:?} in_next_avg: {in_next_avg:?}");
-    let out = buffers.first_mut().unwrap();
+    let Some(out) = buffers.first_mut() else {
+        return;
+    };
     let mut out_rect = ImageRectMut::<i32>::from_raw(out.data.as_rect_mut());
     // Shortcut: guarantees that row is at least 1px in the main loop
     if out_rect.size().0 == 0 || out_rect.size().1 == 0 {
@@ -847,8 +854,11 @@ fn do_hsqueeze_step_i32(
     let w = in_res.size().0;
     // Another shortcut: when output row has just 1px
     if w == 0 {
+        if in_avg.size().0 == 0 || in_avg.size().1 == 0 {
+            return;
+        }
         let out_h = out_rect.size().1;
-        for y in 0..out_h {
+        for y in 0..out_h.min(in_avg.size().1) {
             out_rect.row(y)[0] = in_avg.row(y)[0];
         }
         return;
@@ -1255,7 +1265,9 @@ fn do_vsqueeze_step_i16(
     buffers: &mut [&mut ModularChannel],
 ) {
     trace!("vsqueeze step in_avg: {in_avg:?} in_res: {in_res:?} in_next_avg: {in_next_avg:?}");
-    let out = buffers.first_mut().unwrap();
+    let Some(out) = buffers.first_mut() else {
+        return;
+    };
     let mut out_rect = ImageRectMut::<i16>::from_raw(out.data.as_rect_mut());
     // Shortcut: guarantees that there at least 1 output row
     if out_rect.size().1 == 0 || out_rect.size().0 == 0 {
@@ -1263,8 +1275,11 @@ fn do_vsqueeze_step_i16(
     }
     // Another shortcut: when there is one output row
     if in_res.size().1 == 0 {
-        let w = in_avg.size().0;
-        out_rect.row(0)[..w].copy_from_slice(in_avg.row(0));
+        if in_avg.size().1 == 0 || in_avg.size().0 == 0 {
+            return;
+        }
+        let w = in_avg.size().0.min(out_rect.size().0);
+        out_rect.row(0)[..w].copy_from_slice(&in_avg.row(0)[..w]);
         return;
     }
     // Otherwise: 2 or more rows
@@ -1281,7 +1296,9 @@ fn do_vsqueeze_step_i32(
     buffers: &mut [&mut ModularChannel],
 ) {
     trace!("vsqueeze step in_avg: {in_avg:?} in_res: {in_res:?} in_next_avg: {in_next_avg:?}");
-    let out = buffers.first_mut().unwrap();
+    let Some(out) = buffers.first_mut() else {
+        return;
+    };
     let mut out_rect = ImageRectMut::<i32>::from_raw(out.data.as_rect_mut());
     // Shortcut: guarantees that there at least 1 output row
     if out_rect.size().1 == 0 || out_rect.size().0 == 0 {
@@ -1289,7 +1306,11 @@ fn do_vsqueeze_step_i32(
     }
     // Another shortcut: when there is one output row
     if in_res.size().1 == 0 {
-        out_rect.row(0).copy_from_slice(in_avg.row(0));
+        if in_avg.size().1 == 0 || in_avg.size().0 == 0 {
+            return;
+        }
+        let w = in_avg.size().0.min(out_rect.size().0);
+        out_rect.row(0)[..w].copy_from_slice(&in_avg.row(0)[..w]);
         return;
     }
     // Otherwise: 2 or more rows
