diff --git a/src/audio.rs b/src/audio.rs
index a0c9c3f..0a18f68 100644
--- a/src/audio.rs
+++ b/src/audio.rs
@@ -727,9 +727,9 @@ fn residue_packet_decode_inner(rdr :&mut BitpackCursor, cur_blocksize :u16,
 								Err(err) => {
 									use ::header::HuffmanVqReadErr::*;
 									match err {
-										EndOfPacket => break 'pseudo_return,
-										NoVqLookupForCodebook =>
-											panic!("Codebook must have a value mapping"),
+                                        EndOfPacket => break 'pseudo_return,
+                                        // Hostile streams can still reference non-VQ codebooks.
+                                        NoVqLookupForCodebook => return Err(()),
 									}
 								},
 							}
diff --git a/src/huffman_tree.rs b/src/huffman_tree.rs
--- a/src/huffman_tree.rs
+++ b/src/huffman_tree.rs
@@ -386,7 +386,17 @@ impl<'a> VorbisHuffmanIter<'a> {
 		assert!((cur_entry & (1u32 << 31)) != 0);*/
 
 		//print!("With bit {:?}, pos {:?} becomes pos ", bit, self.pos);
-		self.pos = self.desc_prog[self.pos as usize + 1 + bit as usize];
+		let next_idx = self.pos as usize + 1 + bit as usize;
+		if next_idx >= self.desc_prog.len() {
+			self.pos = 0;
+			return None;
+		}
+		let next_pos = self.desc_prog[next_idx] as usize;
+		if next_pos >= self.desc_prog.len() {
+			self.pos = 0;
+			return None;
+		}
+		self.pos = next_pos as u32;
 		//print!("{:?}", self.pos);
 		let child = self.desc_prog[self.pos as usize];
 		if (child & (1u32 << 31)) != 0 {
