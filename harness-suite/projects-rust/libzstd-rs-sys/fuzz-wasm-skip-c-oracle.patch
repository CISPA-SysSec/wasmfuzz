diff --git a/fuzz/fuzz_targets/decompress.rs b/fuzz/fuzz_targets/decompress.rs
index fbdc852..8347d2c 100644
--- a/fuzz/fuzz_targets/decompress.rs
+++ b/fuzz/fuzz_targets/decompress.rs
@@ -62,20 +62,24 @@ fn c(compressed: &[u8]) -> (usize, Vec<u8>) {
 }
 
 fuzz_target!(|data: &[u8]| {
-    let (c_err, c_out) = c(data);
     let (rs_err, rs_out) = rs(data);
 
     let rs_err = to_result(rs_err);
-    let c_err = to_result(c_err);
 
     // The zstd version that we're testing against supports much older legacy versions.
     if rs_err == Err(Error::prefix_unknown) {
         return;
     }
 
-    assert_eq!(rs_err, c_err);
+    #[cfg(not(target_family = "wasm"))]
+    {
+        let (c_err, c_out) = c(data);
+        let c_err = to_result(c_err);
 
-    if rs_err.is_ok() {
-        assert_eq!(rs_out, c_out);
+        assert_eq!(rs_err, c_err);
+
+        if rs_err.is_ok() {
+            assert_eq!(rs_out, c_out);
+        }
     }
 });
diff --git a/fuzz/fuzz_targets/decompress_overlapping.rs b/fuzz/fuzz_targets/decompress_overlapping.rs
index f953e91..aac5996 100644
--- a/fuzz/fuzz_targets/decompress_overlapping.rs
+++ b/fuzz/fuzz_targets/decompress_overlapping.rs
@@ -106,20 +106,24 @@ fn c(compressed: &[u8]) -> (usize, Vec<u8>) {
 }
 
 fuzz_target!(|data: &[u8]| {
-    let (c_err, c_out) = c(data);
     let (rs_err, rs_out) = rs(data);
 
     let rs_err = to_result(rs_err);
-    let c_err = to_result(c_err);
 
     // The zstd version that we're testing against supports much older legacy versions.
     if rs_err == Err(Error::prefix_unknown) {
         return;
     }
 
-    assert_eq!(rs_err, c_err);
+    #[cfg(not(target_family = "wasm"))]
+    {
+        let (c_err, c_out) = c(data);
+        let c_err = to_result(c_err);
 
-    if rs_err.is_ok() {
-        assert_eq!(rs_out, c_out);
+        assert_eq!(rs_err, c_err);
+
+        if rs_err.is_ok() {
+            assert_eq!(rs_out, c_out);
+        }
     }
 });
diff --git a/fuzz/src/lib.rs b/fuzz/src/lib.rs
index 4c3ca44..41ed12e 100644
--- a/fuzz/src/lib.rs
+++ b/fuzz/src/lib.rs
@@ -55,7 +55,8 @@ impl arbitrary::Arbitrary<'_> for ArbitrarySamples {
 #[macro_export]
 macro_rules! assert_eq_rs_c {
     ($tt:tt) => {{
-        #[cfg(not(miri))]
+        // Wasm: skip strict C vs Rust parity (dict training + decompress errors); native asserts.
+        #[cfg(all(not(miri), not(target_family = "wasm")))]
         #[allow(clippy::macro_metavars_in_unsafe)]
         let _c = unsafe {
             use zstd_sys::*;
@@ -72,7 +73,7 @@ macro_rules! assert_eq_rs_c {
             $tt
         };
 
-        #[cfg(not(miri))]
+        #[cfg(all(not(miri), not(target_family = "wasm")))]
         assert_eq!(_rs, _c);
 
         _rs
