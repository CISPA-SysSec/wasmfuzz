diff --git a/symphonia-codec-adpcm/src/lib.rs b/symphonia-codec-adpcm/src/lib.rs
index 6cba413..d414464 100644
--- a/symphonia-codec-adpcm/src/lib.rs
+++ b/symphonia-codec-adpcm/src/lib.rs
@@ -26,10 +26,13 @@ use symphonia_core::codecs::audio::well_known::{
 };
 use symphonia_core::codecs::audio::{AudioCodecId, AudioCodecParameters, AudioDecoderOptions};
 use symphonia_core::codecs::audio::{AudioDecoder, FinalizeResult};
-use symphonia_core::errors::{Result, unsupported_error};
+use symphonia_core::errors::{Result, decode_error, unsupported_error};
 use symphonia_core::io::ReadBytes;
 use symphonia_core::packet::Packet;
 
+/// Upper bound on frames per packet for buffer preallocation (hostile `max_frames_per_packet`).
+const MAX_ADPCM_PACKET_FRAMES: usize = 65_536;
+
 mod codec_ima_qt;
 mod codec_ima_wav;
 mod codec_ms;
@@ -85,6 +88,10 @@ impl AdpcmDecoder {
             _ => return unsupported_error("adpcm: maximum frames per packet is required"),
         };
 
+        if frames > MAX_ADPCM_PACKET_FRAMES {
+            return decode_error("adpcm: max frames per packet exceeds limit");
+        }
+
         if params.frames_per_block.is_none() || params.frames_per_block.unwrap() == 0 {
             return unsupported_error("adpcm: valid frames per block is required");
         }
@@ -126,8 +133,16 @@ impl AdpcmDecoder {
 
         let block_count = packet.block_dur().get() as usize / frames_per_block;
 
+        let total_frames = block_count.saturating_mul(frames_per_block);
+        if total_frames > MAX_ADPCM_PACKET_FRAMES {
+            return decode_error("adpcm: packet frame count exceeds limit");
+        }
+        if total_frames > self.buf.capacity() {
+            return decode_error("adpcm: packet frame count exceeds output buffer capacity");
+        }
+
         self.buf.clear();
-        self.buf.render_uninit(Some(block_count * frames_per_block));
+        self.buf.render_uninit(Some(total_frames));
 
         let channel_count = self.buf.spec().channels().count();
         match channel_count {
diff --git a/symphonia-codec-adpcm/src/codec_ima_wav.rs b/symphonia-codec-adpcm/src/codec_ima_wav.rs
index 687cc3b..d3e8993 100644
--- a/symphonia-codec-adpcm/src/codec_ima_wav.rs
+++ b/symphonia-codec-adpcm/src/codec_ima_wav.rs
@@ -44,6 +44,9 @@ pub(crate) fn decode_stereo<B: ReadBytes>(
     buffers: [&mut [i32]; 2],
     frames_per_block: usize,
 ) -> Result<()> {
+    if frames_per_block == 0 || (frames_per_block - 1) % 8 != 0 {
+        return decode_error("adpcm (ima): frames_per_block - 1 must be a multiple of 8");
+    }
     let data_bytes_per_channel = frames_per_block - 1;
     let mut status = [read_preamble(stream)?, read_preamble(stream)?];
     buffers[0][0] = from_i16_shift!(status[0].predictor);
diff --git a/symphonia-codec-pcm/src/lib.rs b/symphonia-codec-pcm/src/lib.rs
index 60f660f..2e44224 100644
--- a/symphonia-codec-pcm/src/lib.rs
+++ b/symphonia-codec-pcm/src/lib.rs
@@ -47,6 +47,9 @@ use symphonia_core::errors::{Result, decode_error, unsupported_error};
 use symphonia_core::io::ReadBytes;
 use symphonia_core::packet::Packet;
 
+/// Upper bound on frames per packet for buffer preallocation and decode growth (hostile metadata).
+const MAX_PCM_PACKET_FRAMES: usize = 65_536;
+
 macro_rules! read_pcm_signed {
     ($buf:expr, $fmt:tt, $read:expr, $width:expr, $shift:expr) => {
         // Get buffer of the correct sample format.
@@ -243,6 +246,9 @@ impl PcmDecoder {
         // If the maximum number of frames is known, use it to pre-allocate the audio buffer.
         // Otherwise, start with a 0 capacity and let the buffer grow dynamically while decoding.
         let capacity = params.max_frames_per_packet.unwrap_or(0) as usize;
+        if capacity > MAX_PCM_PACKET_FRAMES {
+            return decode_error("pcm: max frames per packet exceeds limit");
+        }
 
         // Determine the sample format for the audio buffer based on the codec ID.
         let (sample_format, sample_format_width, bytes_per_coded_sample) = match params.codec {
@@ -318,6 +324,9 @@ impl PcmDecoder {
     fn decode_inner(&mut self, packet: &Packet) -> Result<()> {
         // Calculate the number of complete audio frames per-packet.
         let num_frames = packet.buf().len() / self.bytes_per_coded_frame;
+        if num_frames > MAX_PCM_PACKET_FRAMES {
+            return decode_error("pcm: packet frame count exceeds limit");
+        }
 
         // Clear and grow the audio buffer to the required capacity.
         self.buf.clear();
diff --git a/symphonia-codec-vorbis/src/codebook.rs b/symphonia-codec-vorbis/src/codebook.rs
index 1cb2ac0..36d90ad 100644
--- a/symphonia-codec-vorbis/src/codebook.rs
+++ b/symphonia-codec-vorbis/src/codebook.rs
@@ -305,6 +305,10 @@ impl VorbisCodebook {
         // Read and unpack vector quantization (VQ) lookup table.
         let lookup_type = bs.read_bits_leq32(4)?;
 
+        if codebook_dimensions == 0 && (lookup_type & 0xf) != 0 {
+            return decode_error("vorbis: zero-dimension codebook with VQ lookup");
+        }
+
         let vq_vec = match lookup_type & 0xf {
             0 => None,
             1 | 2 => {
@@ -153,6 +153,9 @@ fn synthesize_codewords(code_lens: &[u8]) -> Result<Vec<u32>> {
             // codewords of length N will use the next codeword for codewords of length N-1 as its
             // prefix.
             if next_codeword[i] & 1 == 1 {
+                if i == 0 {
+                    return decode_error("vorbis: invalid codebook codeword assignment");
+                }
                 next_codeword[i] = next_codeword[i - 1] << 1;
                 break;
             }
diff --git a/symphonia-format-caf/src/chunks.rs b/symphonia-format-caf/src/chunks.rs
index 63ec093..994fbda 100644
--- a/symphonia-format-caf/src/chunks.rs
+++ b/symphonia-format-caf/src/chunks.rs
@@ -6,7 +6,7 @@
 // file, You can obtain one at https://mozilla.org/MPL/2.0/.
 
 use log::{debug, error, info, warn};
-use std::{convert::TryFrom, fmt, mem::size_of, str};
+use std::{fmt, mem::size_of, str};
 use symphonia_core::{
     audio::{AmbisonicBFormat, ChannelLabel, Channels, Position, layouts},
     codecs::audio::{AudioCodecId, well_known::*},
@@ -58,6 +58,22 @@ const CHANNEL_LABEL_DISCRETE_65535: u32 = (1 << 16) | 65535;
 const CHANNEL_LABEL_HOA_ACN_0: u32 = (2 << 16) | 0;
 const CHANNEL_LABEL_HOA_ACN_65024: u32 = (2 << 16) | 65024;
 
+/// Maximum payload size for a `kuki` (magic cookie) chunk.
+const MAX_MAGIC_COOKIE_BYTES: i64 = 32 * 1024 * 1024;
+
+/// Maximum size of one CAF audio packet in variable-length modes (packet table + `next_packet`).
+pub(crate) const MAX_CAF_PACKET_PAYLOAD_BYTES: u64 = 32 * 1024 * 1024;
+
+#[inline]
+pub(crate) fn limit_caf_packet_size(size: u64) -> Result<()> {
+    if size > MAX_CAF_PACKET_PAYLOAD_BYTES {
+        decode_error("caf: packet size exceeds limit")
+    }
+    else {
+        Ok(())
+    }
+}
+
 #[derive(Debug)]
 pub enum Chunk {
     AudioDescription(AudioDescription),
@@ -94,12 +110,11 @@ impl Chunk {
                 Chunk::PacketTable(PacketTable::read(reader, audio_description, chunk_size)?)
             }
             b"kuki" => {
-                if let Ok(chunk_size) = usize::try_from(chunk_size) {
-                    Chunk::MagicCookie(reader.read_boxed_slice_exact(chunk_size)?)
-                }
-                else {
+                if chunk_size < 0 || chunk_size > MAX_MAGIC_COOKIE_BYTES {
                     return invalid_chunk_size_error("Magic Cookie", chunk_size);
                 }
+                let sz = chunk_size as usize;
+                Chunk::MagicCookie(reader.read_boxed_slice_exact(sz)?)
             }
             b"free" => {
                 if chunk_size < 0 {
@@ -153,6 +168,10 @@ impl AudioDescription {
         let bytes_per_packet = reader.read_be_u32()?;
         let frames_per_packet = reader.read_be_u32()?;
 
+        if bytes_per_packet > 0 {
+            limit_caf_packet_size(u64::from(bytes_per_packet))?;
+        }
+
         let channels_per_frame = reader.read_be_u32()?;
         if channels_per_frame == 0 {
             return decode_error("caf: channels per frame must be not be zero");
@@ -478,6 +497,33 @@ impl PacketTable {
             return decode_error("caf: invalid number of packets in the packet table");
         }
 
+        /// Hard cap on packet-table rows so a bogus `total_packets` cannot force billions of
+        /// iterations or unbounded `Vec` growth.
+        pub const MAX_PACKET_TABLE_ENTRIES: i64 = 16 * 1024 * 1024;
+
+        if total_packets > MAX_PACKET_TABLE_ENTRIES {
+            error!(
+                "packet table entry count ({total_packets}) exceeds limit ({MAX_PACKET_TABLE_ENTRIES})"
+            );
+            return decode_error("caf: packet table has too many entries");
+        }
+
+        let data_budget = chunk_size - 24;
+        let min_bytes_per_packet: i64 = match (desc.bytes_per_packet, desc.frames_per_packet) {
+            // Two variable-length integers per packet.
+            (0, 0) => 2,
+            // One variable-length integer per packet.
+            (0, _) | (_, 0) => 1,
+            (_, _) => 0,
+        };
+
+        if min_bytes_per_packet > 0 && total_packets > 0 {
+            let need = (total_packets as u128).saturating_mul(min_bytes_per_packet as u128);
+            if need > data_budget.max(0) as u128 {
+                return decode_error("caf: packet table exceeds chunk size");
+            }
+        }
+
         let valid_frames = reader.read_be_i64()?;
         if valid_frames < 0 {
             error!("invalid number of frames in the packet table ({valid_frames})");
@@ -499,6 +545,7 @@ impl PacketTable {
             (0, 0) => {
                 for _ in 0..total_packets {
                     let size = read_variable_length_integer(reader)?;
+                    limit_caf_packet_size(size)?;
                     let frames = Duration::from(read_variable_length_integer(reader)?);
                     packets.push(CafPacket {
                         size,
@@ -516,6 +563,7 @@ impl PacketTable {
             (0, frames_per_packet) => {
                 for _ in 0..total_packets {
                     let size = read_variable_length_integer(reader)?;
+                    limit_caf_packet_size(size)?;
                     let frames = Duration::from(frames_per_packet);
                     packets.push(CafPacket {
                         size,
@@ -533,6 +581,7 @@ impl PacketTable {
             (bytes_per_packet, 0) => {
                 for _ in 0..total_packets {
                     let size = bytes_per_packet as u64;
+                    limit_caf_packet_size(size)?;
                     let frames = Duration::from(read_variable_length_integer(reader)?);
                     packets.push(CafPacket {
                         size,
diff --git a/symphonia-format-caf/src/demuxer.rs b/symphonia-format-caf/src/demuxer.rs
index 1de22e5..9f3e371 100644
--- a/symphonia-format-caf/src/demuxer.rs
+++ b/symphonia-format-caf/src/demuxer.rs
@@ -148,6 +148,7 @@ impl FormatReader for CafReader<'_> {
             PacketInfo::VariableAudioPacket { packets, current_packet_index } => {
                 if let Some(packet) = packets.get(*current_packet_index) {
                     *current_packet_index += 1;
+                    limit_caf_packet_size(packet.size)?;
                     let buffer = self.reader.read_boxed_slice(packet.size as usize)?;
                     Ok(Some(Packet::new(0, packet.start_frame, packet.frames, buffer)))
                 }
diff --git a/symphonia-format-isomp4/src/atoms/ftyp.rs b/symphonia-format-isomp4/src/atoms/ftyp.rs
index d070bf4..275ac46 100644
--- a/symphonia-format-isomp4/src/atoms/ftyp.rs
+++ b/symphonia-format-isomp4/src/atoms/ftyp.rs
@@ -33,7 +33,8 @@ impl Atom for FtypAtom {
         }
 
         // Major
-        let major = FourCc::new(it.read_quad_bytes()?);
+        let major = FourCc::try_new(it.read_quad_bytes()?)
+            .ok_or(Error::DecodeError("isomp4 (ftyp): invalid FourCC (non-ASCII)"))?;
 
         // Minor
         let minor = it.read_quad_bytes()?;
@@ -47,7 +48,9 @@ impl Atom for FtypAtom {
 
         for _ in 0..n_brands {
             let brand = it.read_quad_bytes()?;
-            compatible.push(FourCc::new(brand));
+            let brand = FourCc::try_new(brand)
+                .ok_or(Error::DecodeError("isomp4 (ftyp): invalid compatible brand FourCC"))?;
+            compatible.push(brand);
         }
 
         Ok(FtypAtom { major, minor, compatible })
diff --git a/symphonia-format-isomp4/src/atoms/hdlr.rs b/symphonia-format-isomp4/src/atoms/hdlr.rs
index a3c4a68..4e108c8 100644
--- a/symphonia-format-isomp4/src/atoms/hdlr.rs
+++ b/symphonia-format-isomp4/src/atoms/hdlr.rs
@@ -56,7 +56,10 @@ impl Atom for HdlrAtom {
             b"subt" => HandlerType::Subtitle,
             b"text" => HandlerType::Text,
             &hdlr => {
-                warn!("unknown handler type {:?}", FourCc::new(hdlr));
+                match FourCc::try_new(hdlr) {
+                    Some(cc) => warn!("unknown handler type {:?}", cc),
+                    None => warn!("unknown handler type {:x?}", hdlr),
+                }
                 HandlerType::Other(hdlr)
             }
         };
diff --git a/symphonia-metadata/src/ape.rs b/symphonia-metadata/src/ape.rs
index 6f17161..653dc99 100644
--- a/symphonia-metadata/src/ape.rs
+++ b/symphonia-metadata/src/ape.rs
@@ -161,6 +161,8 @@ const APEV1_METADATA_INFO: MetadataInfo =
     MetadataInfo { metadata: METADATA_ID_APEV1, short_name: "apev1", long_name: "APEv1" };
 const APEV2_METADATA_INFO: MetadataInfo =
     MetadataInfo { metadata: METADATA_ID_APEV2, short_name: "apev2", long_name: "APEv2" };
+/// Upper bound for one APE item payload to avoid hostile huge allocations.
+const MAX_APE_ITEM_VALUE_BYTES: usize = 32 * 1024 * 1024;
 
 /// The APE tag version.
 #[derive(PartialEq, Eq)]
@@ -251,6 +253,9 @@ impl ApeItem {
     fn read<B: ReadBytes>(reader: &mut B, header: &ApeHeader) -> Result<ApeItem> {
         // The length of the value in bytes.
         let len = reader.read_u32()? as usize;
+        if len > MAX_APE_ITEM_VALUE_BYTES {
+            return decode_error("ape: item value length exceeds limit");
+        }
 
         // Read flags.
         let flags = match header.version {
diff --git a/symphonia-metadata/src/embedded/flac.rs b/symphonia-metadata/src/embedded/flac.rs
index e075197..bf727be 100644
--- a/symphonia-metadata/src/embedded/flac.rs
+++ b/symphonia-metadata/src/embedded/flac.rs
@@ -32,6 +32,11 @@ pub const FLAC_METADATA_INFO: MetadataInfo = MetadataInfo {
     long_name: "Free Lossless Audio Codec",
 };
 
+/// Upper bound for embedded picture payload allocation (FLAC `PICTURE` block).
+const MAX_FLAC_PICTURE_DATA_BYTES: usize = 32 * 1024 * 1024;
+/// Upper bound for MIME type / description strings in a `PICTURE` block.
+const MAX_FLAC_PICTURE_TEXT_FIELD_BYTES: usize = 256 * 1024;
+
 /// Converts a string of bytes to an ASCII string if all characters are within the printable ASCII
 /// range. If a null byte is encounted, the string terminates at that point.
 fn printable_ascii_to_string(bytes: &[u8]) -> Option<String> {
@@ -63,8 +68,10 @@ pub fn read_flac_picture_block<B: ReadBytes>(reader: &mut B) -> Result<Visual> {
     let type_enc = reader.read_be_u32()?;
 
     // Read the Media Type length in bytes.
-    // TODO: Apply a limit.
     let media_type_len = reader.read_be_u32()? as usize;
+    if media_type_len > MAX_FLAC_PICTURE_TEXT_FIELD_BYTES {
+        return decode_error("meta (flac): picture mime-type length exceeds limit");
+    }
 
     // Read the Media Type bytes
     let media_type_buf = reader.read_boxed_slice_exact(media_type_len)?;
@@ -81,8 +88,10 @@ pub fn read_flac_picture_block<B: ReadBytes>(reader: &mut B) -> Result<Visual> {
     let mut tags = vec![];
 
     // Read the description length in bytes.
-    // TODO: Apply a limit.
     let desc_len = reader.read_be_u32()? as usize;
+    if desc_len > MAX_FLAC_PICTURE_TEXT_FIELD_BYTES {
+        return decode_error("meta (flac): picture description length exceeds limit");
+    }
 
     // Read the description bytes.
     let desc_buf = reader.read_boxed_slice_exact(desc_len)?;
@@ -113,8 +122,10 @@ pub fn read_flac_picture_block<B: ReadBytes>(reader: &mut B) -> Result<Visual> {
     let _color_mode = reader.read_be_u32()?;
 
     // Read the image data length in bytes.
-    // TODO: Apply a limit.
     let data_len = reader.read_be_u32()? as usize;
+    if data_len > MAX_FLAC_PICTURE_DATA_BYTES {
+        return decode_error("meta (flac): picture data length exceeds limit");
+    }
 
     // Read the image data.
     let data = reader.read_boxed_slice_exact(data_len)?;
diff --git a/symphonia-metadata/src/embedded/vorbis.rs b/symphonia-metadata/src/embedded/vorbis.rs
index ab3126b..bf78cdf 100644
--- a/symphonia-metadata/src/embedded/vorbis.rs
+++ b/symphonia-metadata/src/embedded/vorbis.rs
@@ -34,6 +34,11 @@ pub const VORBIS_COMMENT_METADATA_INFO: MetadataInfo = MetadataInfo {
     long_name: "Vorbis Comment",
 };
 
+/// Upper bound for one Vorbis vendor/comment string payload allocation.
+const MAX_VORBIS_COMMENT_BYTES: usize = 32 * 1024 * 1024;
+/// Upper bound for declared Vorbis comment entries.
+const MAX_VORBIS_COMMENT_ENTRIES: usize = 1_000_000;
+
 lazy_static! {
     static ref VORBIS_COMMENT_MAP: RawTagParserMap = {
         let mut m: RawTagParserMap = HashMap::new();
@@ -372,26 +377,33 @@ pub fn read_vorbis_comment<B: ReadBytes>(
     side_data: &mut Vec<MetadataSideData>,
 ) -> Result<()> {
     // Read the vendor string length in bytes.
-    let vendor_len = reader.read_u32()?;
+    let vendor_len = reader.read_u32()? as usize;
+    if vendor_len > MAX_VORBIS_COMMENT_BYTES {
+        return decode_error("meta (vorbis): vendor string length exceeds limit");
+    }
 
     // Ignore the vendor string.
-    reader.ignore_bytes(u64::from(vendor_len))?;
+    reader.ignore_bytes(vendor_len as u64)?;
 
     // Map of chapter number to a vector of chapter information.
     let mut chapters: BTreeMap<u32, Vec<ChapterInfo>> = Default::default();
 
     // Read the number of comments.
     let num_comments = reader.read_u32()? as usize;
+    if num_comments > MAX_VORBIS_COMMENT_ENTRIES {
+        return decode_error("meta (vorbis): comment count exceeds limit");
+    }
 
     // Read each comment.
     for _ in 0..num_comments {
         // Read the comment string length in bytes.
-        let comment_length = reader.read_u32()?;
-
-        // TODO: Apply a limit.
+        let comment_length = reader.read_u32()? as usize;
+        if comment_length > MAX_VORBIS_COMMENT_BYTES {
+            return decode_error("meta (vorbis): comment length exceeds limit");
+        }
 
         // Read the comment string.
-        let mut comment_data = vec![0; comment_length as usize];
+        let mut comment_data = vec![0; comment_length];
         reader.read_buf_exact(&mut comment_data)?;
 
         // Parse the Vorbis comment and handle the parsed output.
diff --git a/symphonia-metadata/src/id3v2/frames.rs b/symphonia-metadata/src/id3v2/frames.rs
index e26d57d..bf463c2 100644
--- a/symphonia-metadata/src/id3v2/frames.rs
+++ b/symphonia-metadata/src/id3v2/frames.rs
@@ -27,6 +27,9 @@ mod readers;
 
 use readers::*;
 
+/// Upper bound for one ID3v2 frame body allocation.
+const MAX_ID3V2_FRAME_DATA_BYTES: u32 = 32 * 1024 * 1024;
+
 // The following is a list of all standardized ID3v2.x frames for all ID3v2 major versions and their
 // implementation status ("S" column) in Symphonia.
 //
@@ -537,6 +540,9 @@ pub fn read_id3v2p3_frame<B: ReadBytes>(reader: &mut B) -> Result<FrameResult> {
 
     // The size of the frame's body.
     let data_size = size - flag_data_size;
+    if data_size > MAX_ID3V2_FRAME_DATA_BYTES {
+        return decode_error("id3v2: frame body size exceeds limit");
+    }
 
     // If compression is enabled, read the decompressed size of the frame.
     let _decompressed_size = if is_compressed { Some(reader.read_be_u32()?) } else { None };
@@ -642,6 +648,9 @@ pub fn read_id3v2p4_frame<B: ReadBytes + FiniteStream>(reader: &mut B) -> Result
 
     // The size of the frame's body.
     let data_size = size - flag_data_size;
+    if data_size > MAX_ID3V2_FRAME_DATA_BYTES {
+        return decode_error("id3v2: frame body size exceeds limit");
+    }
 
     // Frame group identifier byte. Used to group a set of frames. The frame group will be added as
     // a sub-field to all produced tags.
diff --git a/symphonia/fuzz/fuzz_targets/decode_any.rs b/symphonia/fuzz/fuzz_targets/decode_any.rs
index 0534d69..0bf5169 100644
--- a/symphonia/fuzz/fuzz_targets/decode_any.rs
+++ b/symphonia/fuzz/fuzz_targets/decode_any.rs
@@ -5,8 +5,11 @@ use symphonia::core::formats::{FormatOptions, TrackType};
 use symphonia::core::io::MediaSourceStream;
 use symphonia::core::meta::MetadataOptions;
 
-fuzz_target!(|data: Vec<u8>| {
-    let data = std::io::Cursor::new(data);
+fuzz_target!(|data: &[u8]| {
+    if data.len() > 512 * 1024 {
+        return;
+    }
+    let data = std::io::Cursor::new(data.to_vec());
 
     let source = MediaSourceStream::new(Box::new(data), Default::default());
 
@@ -32,8 +35,32 @@ fuzz_target!(|data: Vec<u8>| {
                 return;
             };
 
+            let mut audio_params = (*audio_codec_params).clone();
+            let max_frames_per_packet: u64 = if cfg!(target_pointer_width = "32") {
+                4_096
+            } else {
+                65_536
+            };
+
+            #[cfg(target_pointer_width = "32")]
+            if let Some(ref ch) = audio_params.channels {
+                if ch.count() > 16 {
+                    return;
+                }
+            }
+
+            match audio_params.max_frames_per_packet {
+                Some(m) if m > max_frames_per_packet => {
+                    audio_params.max_frames_per_packet = Some(max_frames_per_packet);
+                }
+                None => {
+                    audio_params.max_frames_per_packet = Some(max_frames_per_packet);
+                }
+                _ => {}
+            }
+
             let mut decoder = match symphonia::default::get_codecs()
-                .make_audio_decoder(&audio_codec_params, &Default::default())
+                .make_audio_decoder(&audio_params, &Default::default())
             {
                 Ok(d) => d,
                 Err(_) => return,

diff --git a/symphonia-bundle-flac/src/parser.rs b/symphonia-bundle-flac/src/parser.rs
index 45aca71..23cffd7 100644
--- a/symphonia-bundle-flac/src/parser.rs
+++ b/symphonia-bundle-flac/src/parser.rs
@@ -138,10 +138,10 @@ impl Fragment {
     }
 
     /// Parse the frame header from the fragment.
-    fn parse_header(&self) -> FrameHeader {
+    fn parse_header(&self) -> Result<FrameHeader> {
         let mut reader = BufReader::new(&self.data);
-        let sync = reader.read_be_u16().unwrap();
-        read_frame_header(&mut reader, sync).unwrap()
+        let sync = reader.read_be_u16().map_err(Error::from)?;
+        read_frame_header(&mut reader, sync)
     }
 }
 
@@ -226,10 +226,11 @@ impl PacketBuilder {
         self.frags.push(frag);
     }
 
-    fn try_build(&mut self, stream_info: &StreamInfo, frag: Fragment) -> Option<ParsedPacket> {
+    fn try_build(&mut self, stream_info: &StreamInfo, frag: Fragment) -> Result<Option<ParsedPacket>> {
         let (header, data) = if frag.crc_match {
             // The fragment has a CRC that matches the expected CRC.
-            (frag.parse_header(), frag.data)
+            let header = frag.parse_header()?;
+            (header, frag.data)
         }
         else {
             // The fragment does not have a CRC that matches the expected CRC.
@@ -256,13 +257,14 @@ impl PacketBuilder {
 
                 data.extend_from_slice(&frag.data);
 
-                (self.frags[i].parse_header(), data.into_boxed_slice())
+                let header = self.frags[i].parse_header()?;
+                (header, data.into_boxed_slice())
             }
             else {
                 // A range of fragments has not been found that forms a packet.
                 self.push_fragment(frag);
 
-                return None;
+                return Ok(None);
             }
         };
 
@@ -273,7 +275,7 @@ impl PacketBuilder {
 
         self.last_header = Some(header);
 
-        Some(ParsedPacket { buf: data, sync })
+        Ok(Some(ParsedPacket { buf: data, sync }))
     }
 
     fn reset(&mut self) {
@@ -494,8 +496,9 @@ impl PacketParser {
                 None => return Ok(None),
             };
 
-            if let Some(packet) = self.builder.try_build(&self.info, fragment) {
-                break packet;
+            match self.builder.try_build(&self.info, fragment)? {
+                Some(packet) => break packet,
+                None => {}
             }
         };

diff --git a/symphonia-format-riff/src/wave/chunks.rs b/symphonia-format-riff/src/wave/chunks.rs
--- a/symphonia-format-riff/src/wave/chunks.rs
+++ b/symphonia-format-riff/src/wave/chunks.rs
@@ -593,13 +593,17 @@
 pub struct InfoChunk {
     pub tag: [u8; 4],
     pub buf: Box<[u8]>,
 }
 
 impl ParseChunk for InfoChunk {
     fn parse<B: ReadBytes>(reader: &mut B, tag: [u8; 4], len: u32) -> Result<InfoChunk> {
-        // TODO: Apply limit.
+        // Hard cap: malicious INFO sub-chunks must not force multi-gigabyte allocations.
+        const MAX_RIFF_INFO_CHUNK_BYTES: u32 = 32 * 1024 * 1024;
+        if len > MAX_RIFF_INFO_CHUNK_BYTES {
+            return decode_error("wav: info chunk length exceeds limit");
+        }
         let buf = reader.read_boxed_slice_exact(len as usize)?;
         Ok(InfoChunk { tag, buf })
     }
 }
 
diff --git a/symphonia-format-riff/src/aiff/chunks.rs b/symphonia-format-riff/src/aiff/chunks.rs
--- a/symphonia-format-riff/src/aiff/chunks.rs
+++ b/symphonia-format-riff/src/aiff/chunks.rs
@@ -432,6 +432,10 @@ pub struct TextChunk {
 
 impl ParseChunk for TextChunk {
     fn parse<B: ReadBytes>(reader: &mut B, tag: [u8; 4], len: u32) -> Result<Self> {
+        // wasm32 cannot allocate > isize::MAX (~2 GiB) without aborting on capacity overflow.
+        if len > 16 * 1024 * 1024 {
+            return decode_error("aiff: text chunk too large");
+        }
         let text = reader.read_boxed_slice_exact(len as usize)?;
 
         let value = Arc::new(decode_string(&text));

diff --git a/symphonia-format-riff/src/aiff/chunks.rs b/symphonia-format-riff/src/aiff/chunks.rs
--- a/symphonia-format-riff/src/aiff/chunks.rs
+++ b/symphonia-format-riff/src/aiff/chunks.rs
@@ -372,6 +372,10 @@ impl ParseChunk for AppSpecificChunk {
 impl ParseChunk for AppSpecificChunk {
     fn parse<B: ReadBytes>(reader: &mut B, _tag: [u8; 4], len: u32) -> Result<Self> {
         let start_pos = reader.pos();
+        // `APPL` payload length is attacker-controlled; cap before allocating remainder.
+        if len > 16 * 1024 * 1024 {
+            return decode_error("aiff: app-specific chunk too large");
+        }
 
         // The application signature.
         let signature = reader.read_quad_bytes()?;

diff --git a/symphonia-format-mkv/src/ebml.rs b/symphonia-format-mkv/src/ebml.rs
index dd368ff..d07fbdd 100644
--- a/symphonia-format-mkv/src/ebml.rs
+++ b/symphonia-format-mkv/src/ebml.rs
@@ -60,6 +60,18 @@ impl From<std::io::Error> for EbmlError {
 
 pub type Result<T> = std::result::Result<T, EbmlError>;
 
+
+/// Hard cap on EBML string/binary element payload size (hostile `data_size` on wasm32).
+const MAX_EBML_ELEMENT_BYTES: usize = 32 * 1024 * 1024;
+
+fn check_ebml_element_size(size: usize) -> Result<()> {
+    if size > MAX_EBML_ELEMENT_BYTES {
+        Err(EbmlError::ElementError("mkv: element data size exceeds limit"))
+    } else {
+        Ok(())
+    }
+}
+
 /// A super-trait of `ReadBytes` and `SeekBuffered` that all readers of `EbmlIterator` must
 /// implement.
 pub(crate) trait ReadEbml: ReadBytes + SeekBuffered {}
@@ -792,6 +804,7 @@ impl<R: ReadEbml, S: EbmlSchema> EbmlIterator<R, S> {
                     match size {
                         0 => Ok(None),
                         _ => {
+                            check_ebml_element_size(size)?;
                             let data = self.reader.read_boxed_slice_exact(size)?;
                             self.discard_current();
                             let bytes = data.split(|b| *b == 0).next().unwrap_or(&data);
@@ -830,6 +843,7 @@ impl<R: ReadEbml, S: EbmlSchema> EbmlIterator<R, S> {
                 EbmlDataType::Master => Err(EbmlError::ExpectedNonMasterElement),
                 EbmlDataType::Binary => {
                     let size = element.data_size.ok_or(EbmlError::UnknownElementDataSize)? as usize;
+                    check_ebml_element_size(size)?;
                     let data = self.reader.read_boxed_slice_exact(size)?;
                     self.discard_current();
                     Ok(data)
diff --git a/symphonia-codec-aac/src/aac/ics/mod.rs b/symphonia-codec-aac/src/aac/ics/mod.rs
index 95e1f97..5bb828a 100644
--- a/symphonia-codec-aac/src/aac/ics/mod.rs
+++ b/symphonia-codec-aac/src/aac/ics/mod.rs
@@ -243,6 +243,10 @@ impl Ics {
             let mut l = 0;
 
             while k < self.info.max_sfb {
+                if l >= MAX_SFBS {
+                    return decode_error("aac: too many section bands");
+                }
+
                 self.sect_cb[g][l] = bs.read_bits_leq32(4)? as u8;
                 self.sect_len[g][l] = 0;
 
@@ -260,6 +264,10 @@ impl Ics {
                     }
                 }
 
+                if self.sect_len[g][l] == 0 {
+                    return decode_error("aac: invalid zero-length section");
+                }
+
                 validate!(k + self.sect_len[g][l] <= self.info.max_sfb);
 
                 for sfb in k..k + self.sect_len[g][l] {
diff --git a/symphonia-core/src/io/scoped_stream.rs b/symphonia-core/src/io/scoped_stream.rs
index 6da24fd..9e85e05 100644
--- a/symphonia-core/src/io/scoped_stream.rs
+++ b/symphonia-core/src/io/scoped_stream.rs
@@ -27,7 +27,24 @@ impl<B: ReadBytes> ScopedStream<B> {
     /// Instantiates a new `ScopedStream` with an upper limit on the number of bytes that can be
     /// read from the inner source.
     pub fn new(inner: B, len: u64) -> Self {
-        ScopedStream { start: inner.pos(), inner, len, read: 0 }
+        let start = inner.pos();
+        // Hostile or inconsistent callers may pass a `len` that wraps `start + len`.
+        let len = match start.checked_add(len) {
+            Some(_) => len,
+            None => u64::MAX - start,
+        };
+        ScopedStream { start, inner, len, read: 0 }
+    }
+
+    #[inline]
+    fn scope_bounds(&self) -> (u64, u64) {
+        let end = self.start.saturating_add(self.len);
+        if end >= self.start {
+            (self.start, end)
+        }
+        else {
+            (self.start, self.start)
+        }
     }
 
     /// Returns an immutable reference to the inner stream.
@@ -177,7 +194,8 @@ impl<B: ReadBytes + SeekBuffered> SeekBuffered for ScopedStream<B> {
     #[inline(always)]
     fn seek_buffered(&mut self, pos: u64) -> u64 {
         // Clamp the seekable position to within the bounds of the ScopedStream.
-        self.inner.seek_buffered(pos.clamp(self.start, self.start + self.len))
+        let (lo, hi) = self.scope_bounds();
+        self.inner.seek_buffered(pos.clamp(lo, hi))
     }
 
     #[inline(always)]
diff --git a/symphonia-format-ogg/src/physical.rs b/symphonia-format-ogg/src/physical.rs
index ee76005..60a57fc 100644
--- a/symphonia-format-ogg/src/physical.rs
+++ b/symphonia-format-ogg/src/physical.rs
@@ -147,6 +147,10 @@ fn scan_stream_end(
     streams: &mut BTreeMap<u32, LogicalStream>,
     byte_range_end: u64,
 ) -> Option<u64> {
-    let scoped_len = byte_range_end - reader.pos();
+    let pos = reader.pos();
+    if pos >= byte_range_end {
+        return None;
+    }
+    let scoped_len = byte_range_end - pos;
 
     let mut scoped_reader = ScopedStream::new(reader, scoped_len);
diff --git a/symphonia-format-caf/src/demuxer.rs b/symphonia-format-caf/src/demuxer.rs
--- a/symphonia-format-caf/src/demuxer.rs
+++ b/symphonia-format-caf/src/demuxer.rs
@@ -141,6 +141,7 @@ impl FormatReader for CafReader<'_> {
                     return Ok(None);
                 };
 
+                limit_caf_packet_size(bytes_to_read)?;
                 let buf = self.reader.read_boxed_slice(bytes_to_read as usize)?;
 
                 Ok(Some(Packet::new(0, pts, dur, buf)))
 
diff --git a/symphonia-core/src/audio/buf.rs b/symphonia-core/src/audio/buf.rs
index ddcea78..da1dc48 100644
--- a/symphonia-core/src/audio/buf.rs
+++ b/symphonia-core/src/audio/buf.rs
@@ -350,16 +350,10 @@ impl<S: Sample> AudioBuffer<S> {
     /// If `num_frames` is `None`, the remaining capacity will be used.
     ///
     /// Returns the number of frames rendered.
-    ///
-    /// # Panics
-    ///
-    /// Panics if `num_frames` is `Some` and would exceed the capacity of the buffer.
     pub fn render_silence(&mut self, num_frames: Option<usize>) -> usize {
-        // The number of new frames to render.
-        let num_new_frames = num_frames.unwrap_or(self.capacity - self.num_frames);
-
-        // Do not render past the end of the audio buffer.
-        assert!(self.num_frames + num_new_frames <= self.capacity(), "capacity will be exceeded");
+        // Clamp requests to remaining capacity instead of panicking on malformed streams.
+        let available = self.capacity.saturating_sub(self.num_frames);
+        let num_new_frames = num_frames.unwrap_or(available).min(available);
 
         // Render silence.
         for plane in &mut self.planes {
@@ -382,16 +376,10 @@ impl<S: Sample> AudioBuffer<S> {
     /// If `num_frames` is `None`, the remaining capacity will be used.
     ///
     /// Returns the number of frames rendered.
-    ///
-    /// # Panics
-    ///
-    /// Panics if `num_frames` is `Some` and would exceed the capacity of the buffer.
     pub fn render_uninit(&mut self, num_frames: Option<usize>) -> usize {
-        // The number of new frames to render.
-        let num_new_frames = num_frames.unwrap_or(self.capacity - self.num_frames);
-
-        // Do not render past the end of the audio buffer.
-        assert!(self.num_frames + num_new_frames <= self.capacity(), "capacity will be exceeded");
+        // Clamp requests to remaining capacity instead of panicking on malformed streams.
+        let available = self.capacity.saturating_sub(self.num_frames);
+        let num_new_frames = num_frames.unwrap_or(available).min(available);
 
         // Commit new frames.
         self.num_frames += num_new_frames;

diff --git a/symphonia/fuzz/fuzz_targets/decode_flac.rs b/symphonia/fuzz/fuzz_targets/decode_flac.rs
--- a/symphonia/fuzz/fuzz_targets/decode_flac.rs
+++ b/symphonia/fuzz/fuzz_targets/decode_flac.rs
@@ -6,7 +6,7 @@ use symphonia::core::codecs::audio::{
 use symphonia::default::codecs::FlacDecoder;
 use symphonia_fuzz::fuzz_audio_decoder;
 
-fuzz_target!(|data: Vec<u8>| {
+fuzz_target!(|data: &[u8]| {
     let mut codec_params = AudioCodecParameters::new();
     codec_params.for_codec(CODEC_ID_FLAC);
 
diff --git a/symphonia-format-ogg/src/mappings/vorbis.rs b/symphonia-format-ogg/src/mappings/vorbis.rs
index 2567f59..488555d 100644
--- a/symphonia-format-ogg/src/mappings/vorbis.rs
+++ b/symphonia-format-ogg/src/mappings/vorbis.rs
@@ -482,6 +482,10 @@ pub fn skip_codebook(bs: &mut BitReaderRtl<'_>) -> Result<()> {
     // Read and unpack vector quantization (VQ) lookup table.
     let lookup_type = bs.read_bits_leq32(4)?;
 
+    if codebook_dimensions == 0 && (lookup_type & 0xf) != 0 {
+        return decode_error("ogg (vorbis): zero-dimension codebook with VQ lookup");
+    }
+
     match lookup_type & 0xf {
         0 => (),
         1 | 2 => {
@@ -492,8 +496,11 @@ pub fn skip_codebook(bs: &mut BitReaderRtl<'_>) -> Result<()> {
 
             // Lookup type is either 1 or 2 as per outer match.
             let lookup_values = match lookup_type {
-                1 => lookup1_values(codebook_entries, codebook_dimensions),
-                2 => codebook_entries * u32::from(codebook_dimensions),
+                1 => lookup1_values(codebook_entries, codebook_dimensions)?,
+                2 => match codebook_entries.checked_mul(u32::from(codebook_dimensions)) {
+                    Some(v) => v,
+                    None => return decode_error("ogg (vorbis): codebook lookup size overflow"),
+                },
                 _ => unreachable!(),
             };
 
@@ -722,14 +729,25 @@ pub fn ilog(x: u32) -> u32 {
 }
 
 #[inline(always)]
-fn lookup1_values(entries: u32, dimensions: u16) -> u32 {
-    // (value ^ dimensions) <= entries
-    // [(value ^ dimensions) ^ (1 / dimensions)] = lower[entries ^ (1 / dimensions)]
-    // value = lower[entries ^ (1 / dimensions)]
-    let value = (entries as f32).powf(1.0f32 / f32::from(dimensions)).floor() as u32;
-
-    assert!(value.pow(u32::from(dimensions)) <= entries);
-    assert!((value + 1).pow(u32::from(dimensions)) > entries);
+fn lookup1_values(entries: u32, dimensions: u16) -> Result<u32> {
+    if dimensions == 0 {
+        return decode_error("ogg (vorbis): invalid codebook dimensions for lookup");
+    }
 
-    value
+    let d = u32::from(dimensions);
+    let mut x = 1u32;
+    loop {
+        let xpow = match x.checked_pow(d) {
+            Some(p) => p,
+            None => return decode_error("ogg (vorbis): codebook lookup size overflow"),
+        };
+        if xpow > entries {
+            break;
+        }
+        x = x.saturating_add(1);
+        if x == u32::MAX {
+            return decode_error("ogg (vorbis): invalid codebook lookup size");
+        }
+    }
+    Ok(x - 1)
 }
