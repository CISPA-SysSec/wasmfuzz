set -e
git clone-rev.sh https://github.com/pdeljanov/symphonia.git "$PROJECT/repo" ee35874b571a35a9a6e15d3bc9a3aaf8f11fbeee
git -C "$PROJECT/repo" apply "$PROJECT/limit-adpcm-packet-frames.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-pcm-packet-frames.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-vorbis-codebook-lookup.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-vorbis-residue-bounds.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-vorbis-floor0-book-idx.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-caf-packet-size.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-ape-item-value.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-flac-picture-metadata.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-vorbis-comment-metadata.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-id3v2-frame-data.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fuzz-decode-any-limits.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fuzz-decode-flac-slice-input.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-wav-info-chunk.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-aiff-chunk-size.patch"
git -C "$PROJECT/repo" apply "$PROJECT/limit-ebml-element-size.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-scoped-stream-overflow.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-ogg-physical-scoped-len.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-audio-buffer-render-clamp.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-ogg-vorbis-codebook-skip.patch"
git -C "$PROJECT/repo" apply "$PROJECT/fix-empty-codebook-bitreader.patch"
