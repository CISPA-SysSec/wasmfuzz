diff --git a/Cargo.toml b/Cargo.toml
index 378b39e..a2c2c88 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -156,3 +156,10 @@ harness = false
 # TODO: remove when shipping the next major release after 0.25
 [package.metadata.cargo-semver-checks.lints]
 enum_discriminants_undefined_non_unit_variant = { level = "warn" }
+
+[patch.crates-io]
+zune-jpeg = { path = "../zune-jpeg" }
+image-webp = { path = "../image-webp" }
+png = { path = "../png" }
+tiff = { path = "../tiff" }
+moxcms = { path = "../moxcms" }
diff --git a/fuzz/Cargo.toml b/fuzz/Cargo.toml
index a338e73..9e4d7bb 100644
--- a/fuzz/Cargo.toml
+++ b/fuzz/Cargo.toml
@@ -18,6 +18,13 @@ version = "0.4"
 [workspace]
 members = ["."]
 
+[patch.crates-io]
+zune-jpeg = { path = "../../zune-jpeg" }
+image-webp = { path = "../../image-webp" }
+png = { path = "../../png" }
+tiff = { path = "../../tiff" }
+moxcms = { path = "../../moxcms" }
+
 [[bin]]
 name = "fuzzer_script_guess"
 path = "fuzzers/fuzzer_script_guess.rs"
diff --git a/fuzz/fuzzers/fuzzer_script_guess.rs b/fuzz/fuzzers/fuzzer_script_guess.rs
index 7f8fbf5..7dbb87f 100644
--- a/fuzz/fuzzers/fuzzer_script_guess.rs
+++ b/fuzz/fuzzers/fuzzer_script_guess.rs
@@ -2,6 +2,18 @@
 #[macro_use] extern crate libfuzzer_sys;
 extern crate image;
 
+use image::{ImageReaderOptions, Limits};
+use std::io::Cursor;
+
 fuzz_target!(|data: &[u8]| {
-    let _ = image::load_from_memory(data);
+    let mut reader = match ImageReaderOptions::new(Cursor::new(data)).with_guessed_format() {
+        Ok(r) => r,
+        Err(_) => return,
+    };
+    let mut limits = Limits::default();
+    limits.max_alloc = Some(16 * 1024 * 1024);
+    limits.max_image_width = Some(8192);
+    limits.max_image_height = Some(8192);
+    reader.limits(limits);
+    let _ = reader.decode();
 });
diff --git a/fuzz/fuzzers/fuzzer_script_jpeg.rs b/fuzz/fuzzers/fuzzer_script_jpeg.rs
index 600f9a1..28ab9cf 100644
--- a/fuzz/fuzzers/fuzzer_script_jpeg.rs
+++ b/fuzz/fuzzers/fuzzer_script_jpeg.rs
@@ -1,7 +1,21 @@
 #![no_main]
 #[macro_use] extern crate libfuzzer_sys;
 extern crate image;
+use image::ImageDecoder;
+use std::io::Cursor;
 
 fuzz_target!(|data: &[u8]| {
-    let _ = image::load_from_memory_with_format(data, image::ImageFormat::Jpeg);
+    let mut decoder = image::codecs::jpeg::JpegDecoder::new(Cursor::new(data));
+
+    let layout = match decoder.prepare_image() {
+        Ok(layout) => layout,
+        Err(_) => return,
+    };
+
+    if layout.total_bytes() > 32 * 1024 * 1024 {
+        return;
+    }
+
+    let mut buf = vec![0; layout.total_bytes() as usize];
+    let _ = decoder.read_image(&mut buf);
 });
diff --git a/fuzz/fuzzers/fuzzer_script_png.rs b/fuzz/fuzzers/fuzzer_script_png.rs
index 6b25ac4..9a0c537 100644
--- a/fuzz/fuzzers/fuzzer_script_png.rs
+++ b/fuzz/fuzzers/fuzzer_script_png.rs
@@ -1,7 +1,21 @@
 #![no_main]
 #[macro_use] extern crate libfuzzer_sys;
 extern crate image;
+use image::ImageDecoder;
+use std::io::Cursor;
 
 fuzz_target!(|data: &[u8]| {
-    let _ = image::load_from_memory_with_format(data, image::ImageFormat::Png);
+    let mut decoder = image::codecs::png::PngDecoder::new(Cursor::new(data));
+
+    let layout = match decoder.prepare_image() {
+        Ok(layout) => layout,
+        Err(_) => return,
+    };
+
+    if layout.total_bytes() > 256 * 1024 * 1024 {
+        return;
+    }
+
+    let mut buf = vec![0; layout.total_bytes() as usize];
+    let _ = decoder.read_image(&mut buf);
 });
diff --git a/fuzz/fuzzers/fuzzer_script_tiff.rs b/fuzz/fuzzers/fuzzer_script_tiff.rs
index 7450dae..64de05b 100644
--- a/fuzz/fuzzers/fuzzer_script_tiff.rs
+++ b/fuzz/fuzzers/fuzzer_script_tiff.rs
@@ -1,7 +1,24 @@
 #![no_main]
 #[macro_use] extern crate libfuzzer_sys;
 extern crate image;
+use image::ImageDecoder;
+use std::io::Cursor;
 
 fuzz_target!(|data: &[u8]| {
-    let _ = image::load_from_memory_with_format(data, image::ImageFormat::Tiff);
+    let mut decoder = match image::codecs::tiff::TiffDecoder::new(Cursor::new(data)) {
+        Ok(decoder) => decoder,
+        Err(_) => return,
+    };
+
+    let layout = match decoder.prepare_image() {
+        Ok(layout) => layout,
+        Err(_) => return,
+    };
+
+    if layout.total_bytes() > 256 * 1024 * 1024 {
+        return;
+    }
+
+    let mut buf = vec![0; layout.total_bytes() as usize];
+    let _ = decoder.read_image(&mut buf);
 });
diff --git a/fuzz/fuzzers/fuzzer_script_webp.rs b/fuzz/fuzzers/fuzzer_script_webp.rs
index 4ba62ba..76abca1 100644
--- a/fuzz/fuzzers/fuzzer_script_webp.rs
+++ b/fuzz/fuzzers/fuzzer_script_webp.rs
@@ -1,7 +1,24 @@
 #![no_main]
 #[macro_use] extern crate libfuzzer_sys;
 extern crate image;
+use image::ImageDecoder;
+use std::io::Cursor;
 
 fuzz_target!(|data: &[u8]| {
-    let _ = image::load_from_memory_with_format(data, image::ImageFormat::WebP);
+    let mut decoder = match image::codecs::webp::WebPDecoder::new(Cursor::new(data)) {
+        Ok(decoder) => decoder,
+        Err(_) => return,
+    };
+
+    let layout = match decoder.prepare_image() {
+        Ok(layout) => layout,
+        Err(_) => return,
+    };
+
+    if layout.total_bytes() > 256 * 1024 * 1024 {
+        return;
+    }
+
+    let mut buf = vec![0; layout.total_bytes() as usize];
+    let _ = decoder.read_image(&mut buf);
 });
diff --git a/src/codecs/webp/decoder.rs b/src/codecs/webp/decoder.rs
index 4c4b13b..4dea16b 100644
--- a/src/codecs/webp/decoder.rs
+++ b/src/codecs/webp/decoder.rs
@@ -2,7 +2,10 @@ use std::io::{BufRead, Seek};
 
 use image_webp::LoopCount;
 
-use crate::error::{DecodingError, ImageError, ImageResult, ParameterError, ParameterErrorKind};
+use crate::error::{
+    DecodingError, ImageError, ImageResult, LimitError, LimitErrorKind, ParameterError,
+    ParameterErrorKind,
+};
 use crate::io::{
     DecodedAnimationAttributes, DecodedImageAttributes, DecodedMetadataHint, DecoderPreparedImage,
     FormatAttributes, SequenceControl,
@@ -40,6 +43,18 @@ impl<R: BufRead + Seek> WebPDecoder<R> {
 }
 
 impl<R: BufRead + Seek> ImageDecoder for WebPDecoder<R> {
+    fn set_limits(&mut self, limits: crate::Limits) -> ImageResult<()> {
+        limits.check_support(&crate::LimitSupport::default())?;
+        let layout = self.prepare_image()?;
+        limits.check_layout_dimensions(&layout)?;
+        let cap = limits
+            .max_alloc
+            .map(|m| usize::try_from(m).unwrap_or(usize::MAX))
+            .unwrap_or(usize::MAX);
+        self.inner.set_memory_limit(cap);
+        Ok(())
+    }
+
     fn format_attributes(&self) -> FormatAttributes {
         FormatAttributes {
             // As per extended file format description:
@@ -142,6 +157,9 @@ impl ImageError {
             image_webp::DecodingError::NoMoreFrames => {
                 ImageError::Parameter(ParameterError::from_kind(ParameterErrorKind::NoMoreData))
             }
+            image_webp::DecodingError::MemoryLimitExceeded => {
+                ImageError::Limits(LimitError::from_kind(LimitErrorKind::InsufficientMemory))
+            }
             _ => ImageError::Decoding(DecodingError::new(ImageFormat::WebP.into(), e)),
         }
     }
