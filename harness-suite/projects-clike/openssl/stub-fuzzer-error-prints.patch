diff --git a/fuzz/bignum.c b/fuzz/bignum.c
index eec776e..a406389 100644
--- a/fuzz/bignum.c
+++ b/fuzz/bignum.c
@@ -84,6 +84,7 @@ int FuzzerTestOneInput(const uint8_t *buf, size_t len)
         goto done;
 
     if (BN_cmp(b4, b5) != 0) {
+	/*
         BN_print_fp(stdout, b1);
         putchar('\n');
         BN_print_fp(stdout, b2);
@@ -94,6 +95,7 @@ int FuzzerTestOneInput(const uint8_t *buf, size_t len)
         putchar('\n');
         BN_print_fp(stdout, b5);
         putchar('\n');
+	*/
     }
 
 done:
diff --git a/fuzz/bndiv.c b/fuzz/bndiv.c
index c3a2d2f..4d2ea50 100644
--- a/fuzz/bndiv.c
+++ b/fuzz/bndiv.c
@@ -110,6 +110,7 @@ int FuzzerTestOneInput(const uint8_t *buf, size_t len)
 
     success = success && BN_cmp(b5, b1) == 0;
     if (!success) {
+	/*
         BN_print_fp(stdout, b1);
         putchar('\n');
         BN_print_fp(stdout, b2);
@@ -127,6 +128,7 @@ int FuzzerTestOneInput(const uint8_t *buf, size_t len)
                 && (BN_is_negative(b4) || BN_is_zero(b4)),
             BN_cmp(b5, b1));
         puts("----\n");
+	*/
     }
 
 done:
