diff --git a/fuzzing/box_fuzzer.cc b/fuzzing/box_fuzzer.cc
index 114c8dc..c9f2c12 100644
--- a/fuzzing/box_fuzzer.cc
+++ b/fuzzing/box_fuzzer.cc
@@ -34,7 +34,8 @@ extern "C" int LLVMFuzzerTestOneInput(const uint8_t* data, size_t size)
 
   // Use a context for tracking the memory usage and set the reduced limit.
   HeifContext ctx;
-  ctx.get_security_limits()->max_total_memory = UINT64_C(2) * 1024 * 1024 * 1024;
+  ctx.get_security_limits()->max_total_memory = UINT64_C(256) * 1024 * 1024;
+  ctx.get_security_limits()->max_memory_block_size = UINT64_C(128) * 1024 * 1024;
 
   BitstreamRange range(reader, size);
   for (;;) {
diff --git a/fuzzing/file_fuzzer.cc b/fuzzing/file_fuzzer.cc
index b447e0f..3483418 100644
--- a/fuzzing/file_fuzzer.cc
+++ b/fuzzing/file_fuzzer.cc
@@ -105,7 +105,7 @@ extern "C" int LLVMFuzzerTestOneInput(const uint8_t* data, size_t size)
   assert(ctx);
 
   auto* limits = heif_context_get_security_limits(ctx);
-  limits->max_total_memory = UINT64_C(2) * 1024 * 1024 * 1024;
+  limits->max_total_memory = UINT64_C(256) * 1024 * 1024;
   limits->max_memory_block_size = 128 * 1024 * 1024; // 128 MB
 
   // Also bound the image size itself. libheif's memory accounting only covers its own
diff --git a/fuzzing/tile_fuzzer.cc b/fuzzing/tile_fuzzer.cc
index f530bb7..04441dc 100644
--- a/fuzzing/tile_fuzzer.cc
+++ b/fuzzing/tile_fuzzer.cc
@@ -102,7 +102,7 @@ extern "C" int LLVMFuzzerTestOneInput(const uint8_t* data, size_t size)
   assert(ctx);
 
   auto* limits = heif_context_get_security_limits(ctx);
-  limits->max_total_memory = UINT64_C(2) * 1024 * 1024 * 1024;
+  limits->max_total_memory = UINT64_C(256) * 1024 * 1024;
   limits->max_memory_block_size = 128 * 1024 * 1024;
   // Also bound the image size itself. libheif's memory accounting only covers its own
   // allocations, but the codec libraries allocate their frame buffers before libheif
